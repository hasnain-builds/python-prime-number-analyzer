# 🔢 Prime Number Analyzer (CLI)

A simple and clean command-line application built using Python to analyze prime numbers.
The program allows users to check whether a number is prime and to display all prime numbers within a given range.

This project focuses on core logic, optimization, and writing clean, readable Python code.

---

## 🚀 Features
- Check if a single number is prime
- Find all prime numbers up to a given range
- Optimized prime-checking logic using the square root method
- Input validation using exception handling
- Clear and user-friendly CLI output
- Modular and readable code structure

---

## 🛠️ Tech Stack
- Python 3
- Built-in Python libraries only

---

## ▶️ How to Run
1. Clone the repository or download the source code
2. Open a terminal in the project directory
3. Run the program using:

```bash
python main.py
